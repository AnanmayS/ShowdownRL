# Optional one-off environment overrides.
# Most users should run `showdownrl setup` instead.
PS_USERNAME=your_username
PS_PASSWORD=your_password
PS_GUEST=false
PS_SITE=https://play.pokemonshowdown.com/
